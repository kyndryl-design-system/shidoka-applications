# BEGIN SHIDOKA STUDIO NPM CONFIG
@kyndryl-cto:registry=https://npm.pkg.github.com
# Keep credentials out of committed project config.
# Local installs should use a user-level ~/.npmrc token.
# CI should inject an npm auth token via workflow, Docker, or runner config.
# END SHIDOKA STUDIO NPM CONFIG

import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { html } from 'lit';
import '../components/reusable/button';
import '../components/reusable/icon';
import arrowRightIcon from '@carbon/icons/es/arrow--right/16';

<Meta
  title="Reusable/Button"
  component="kyn-button"
  argTypes={{
    kind: {
      description: 'The kind of button to render.',
      control: { type: 'select' },
      options: ['primary', 'secondary'],
      table: {
        type: { summary: 'string' },
        defaultValue: { summary: 'primary' },
        category: 'Properties',
      },
    },
    size: {
      description: 'The button size.',
      control: { type: 'select' },
      options: ['sm', 'md', 'lg'],
      table: {
        type: { summary: 'string' },
        defaultValue: { summary: 'md' },
        category: 'Properties',
      },
    },
    href: {
      description: 'Converts to an &lt;a&gt; tag to create a link.',
      control: { type: 'text' },
      table: {
        type: { summary: 'string' },
        defaultValue: { summary: '' },
        category: 'Properties',
      },
    },
    disabled: {
      description: 'Disables the button.',
      control: { type: 'boolean' },
      table: {
        type: { summary: 'boolean' },
        defaultValue: { summary: 'false' },
        category: 'Properties',
      },
    },
    '@on-click': {
      description:
        'Captures the button click event. Emits the original event in the details.',
      table: {
        category: 'Events',
      },
    },
    unnamed: {
      description: 'The default slot for button content.',
      table: {
        category: 'Slots',
      },
    },
  }}
/>

export const args = {
  kind: 'primary',
  size: 'md',
  href: '',
  disabled: false,
};

export const Template = (args) => {
  return html`
    <kyn-button
      kind=${args.kind}
      href=${args.href}
      size=${args.size}
      ?disabled=${args.disabled}
    >
      Default
    </kyn-button>
  `;
};

export const IconTemplate = (args) => {
  return html`
    <kyn-button
      kind=${args.kind}
      href=${args.href}
      size=${args.size}
      ?disabled=${args.disabled}
    >
      Button
      <kyn-icon .icon=${arrowRightIcon}></kyn-icon>
    </kyn-button>
  `;
};

# Button

Primary UI component for user interaction.

## Playground

<Canvas>
  <Story name="Button" args={args}>
    {Template.bind({})}
  </Story>
</Canvas>

## Properties

<ArgsTable story="Button" />

## Other Examples

### Button with Icon

```js
import arrowRightIcon from '@carbon/icons/es/arrow--right/16';
```

<Canvas>
  <Story name="Button with Icon" args={args}>
    {IconTemplate.bind({})}
  </Story>
</Canvas>

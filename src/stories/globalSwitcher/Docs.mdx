import { Meta, Title, Source } from '@storybook/addon-docs/blocks';
import * as GlobalSwitcherStories from './GlobalSwitcher.stories.js';
import { createGlobalSwitcherPatternSnippet } from './globalSwitcherPattern.js';
import navData from './example_global_switcher_data.json';

<Meta of={GlobalSwitcherStories} name="Docs" />

<Title>Global Switcher</Title>

A **pattern** (not a standalone component) for composing categorical navigation inside the Shidoka header.
It assembles header primitives into flyout sections — dropdowns, tabbed catalogs, multi-column menus, and more.

Copy the full pattern from the **Code** panel on **Slotted HTML** or **JSON-driven** (same single snippet): CSS, HTML, `navData` JSON, icon keys, and render logic.

## Pattern Structure

```
kyn-header
└── kyn-header-nav.global-switcher-nav
    ├── kyn-header-link              ← flyout trigger
    │   └── slot="links"
    │       ├── kyn-header-category  ← simple dropdown
    │       ├── div wrapper          ← mixed layout
    │       ├── kyn-tabs             ← tabbed categorical nav
    │       └── kyn-header-categories← multi-column categorical nav
    └── kyn-header-divider           ← separator between groups
```

## Quick Start

```html
<kyn-header rootUrl="/" appTitle="My Application">
  <kyn-header-nav class="global-switcher-nav">
    <!-- each top-level kyn-header-link becomes a flyout trigger -->
    <!-- slot="links" content determines the flyout type -->
  </kyn-header-nav>
</kyn-header>
```

## Copy the full pattern

<Source
  code={createGlobalSwitcherPatternSnippet(
    navData,
    {
      rootUrl: '/',
      appTitle: 'Application',
    },
    {
      autoOpenFlyout: 'favorites',
      truncateLinks: true,
    }
  )}
/>

Optional max flyout height — add to the `.global-switcher-nav` rule in section **[1]**:

```css
--kyn-global-switcher-max-height: 70vh;
```

Set on `kyn-header-nav` (all flyouts) or on a specific `kyn-header-link` (single flyout).

## Link truncation

Ellipsize long labels in flyout menus:

```html
<kyn-header-nav
  class="global-switcher-nav"
  truncate-links
  auto-open-flyout="favorites"
>
  <!-- nested kyn-header-link labels truncate when they overflow -->
</kyn-header-nav>
```

Open **Favorites** in the story to see a long item (for example “Visualization, Exploration and Semantic Analytics”).
For external links, also set `truncate` on the leaf `kyn-header-link` when using `target="_blank"`.

## Section Patterns

Every flyout starts with a `kyn-header-link` trigger. What you slot into `slot="links"` determines the layout.
Section **[2]** shows one trimmed exemplar per `section.type` (not the full nav tree). Section **[4]** iterates all of `navData.sections`.

### Simple Dropdown

A flat `kyn-header-category` — best for short lists like Favorites or Recently Viewed.

### Mixed Layout

A `div` wrapper combining loose links with a categorized group beneath them.

### Tabbed Categorical Nav

`kyn-tabs` with `kyn-header-categories` inside each panel, for large service catalogs.
Use `full-width-flyout` for tabbed categorical Global Switcher flyouts so the
flyout keeps the categorical full-width layout even when the active tab renders
an empty state with no categories.

`kyn-tabs` does not measure or equalize tab widths by itself. Set `--global-switcher-tab-width` on `.global-switcher-nav` (default `170px`), add `class="global-switcher-tabs"` on `kyn-tabs`, and use `fill-width` on each `kyn-tab` (tab width comes from the CSS variable).

### Multi-Column Categorical Nav

`kyn-header-categories` directly in `slot="links"` — for categorized admin-style menus.

## Favorites (Icon Selectors)

Add `kyn-icon-selector` inside leaf links for star-toggle behavior.
The pattern styles above set hover-to-reveal behavior on `kyn-header-nav`:

```html
<kyn-header-link href="/page">
  <span>Page Name</span>
  <kyn-icon-selector>
    <span slot="icon-unchecked">{starOutline}</span>
    <span slot="icon-checked">{starFilled}</span>
  </kyn-icon-selector>
</kyn-header-link>
```

## External Links

Set `target="_blank"` and include a launch icon:

```html
<kyn-header-link href="https://external.example.com" target="_blank" truncate>
  <span>External Tool</span>
  <span style="display: flex; width: 16px; height: 16px; flex-shrink: 0;"
    >{launchIcon}</span
  >
</kyn-header-link>
```

## Key Properties

| Component               | Property            | Default | Description                                                                                            |
| ----------------------- | ------------------- | ------- | ------------------------------------------------------------------------------------------------------ |
| `kyn-header-nav`        | `autoOpenFlyout`    | `''`    | `'default'` auto-opens first flyout; `'<id>'` opens matching flyout; keeps flyouts open on mouse leave |
| `kyn-header-nav`        | `truncateLinks`     | `false` | Cascade truncation to all nested links                                                                 |
| `kyn-header-link`       | `hideSearch`        | `false` | Suppress search in flyout                                                                              |
| `kyn-header-link`       | `searchThreshold`   | `6`     | Child count before search appears                                                                      |
| `kyn-header-link`       | `truncate`          | `false` | Truncate text with ellipsis                                                                            |
| `kyn-header-link`       | `fullWidthFlyout`   | `false` | Opt a Global Switcher tabbed categorical flyout into full-width desktop layout                         |
| `kyn-tab`               | `fillWidth`         | `false` | Allow a parent pattern to size a tab host while the internal tab button fills that width               |
| `kyn-header-categories` | `layout`            | `''`    | `'masonry'` or `'grid'` (empty = legacy responsive columns)                                            |
| `kyn-header-categories` | `maxColumns`        | `3`     | Column cap for masonry/grid layout                                                                     |
| `kyn-header-categories` | `fixedColumnWidths` | `false` | For 3+ columns, keep fixed column widths instead of stretching to full flyout width                    |
| `kyn-header-categories` | `limitRootLinks`    | `true`  | Enable/disable root-link count limiting and "More" behavior                                            |
| `kyn-header-categories` | `maxRootLinks`      | `4`     | Links shown before "More" button (used when `limitRootLinks=true`)                                     |

## Data-Driven Rendering

Production apps typically fetch nav config from a service. Use `example_global_switcher_data.json` as the schema reference.

| `type`        | Pattern                      |
| ------------- | ---------------------------- |
| `simple`      | Simple Dropdown              |
| `mixed`       | Mixed Layout                 |
| `tabbed`      | Tabbed Categorical Nav       |
| `categorical` | Multi-Column Categorical Nav |

Sections **[3]**–**[4]** contain full `navData` and the `sections[].type` render switch (with `.global-switcher-*` layout classes).

Storybook maintainers: `globalSwitcherPattern.js` · `globalSwitcherRender.js` · `globalSwitcherPatternStyles.js`.

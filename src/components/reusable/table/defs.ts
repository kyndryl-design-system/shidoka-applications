export enum TABLE_CELL_ALIGN {
  CENTER = 'center',
  LEFT = 'left',
  RIGHT = 'right',
  INHERIT = 'inherit',
  JUSTIFY = 'justify',
}

export enum SORT_DIRECTION {
  DEFAULT = '',
  ASC = 'asc',
  DESC = 'desc',
}
/**
 * Minimum width allowed for resizable columns (in pixels).
 * Prevents columns from becoming too narrow to be usable.
 */
export const RESIZE_MIN_WIDTH = '50px';

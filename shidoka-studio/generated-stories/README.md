# Generated stories (legacy)

Generated page stories are now written to **`src/stories/pages/`** and that directory is cleared on `npm run dev` and `git push`.

This folder is no longer cleared automatically. Prefer generating into `src/stories/pages/` so imports resolve correctly and Storybook loads the stories.
